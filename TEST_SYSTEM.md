# 🧪 دليل اختبار النظام

## الخطوات:

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. تشغيل النظام
```bash
# مع Backend (موصى به)
npm run dev:all

# أو بدون Backend
npm run dev
```

### 3. اختبار الوظائف

#### أ. اختبار تسجيل الدخول:
1. افتح: `http://localhost:5173/admin/gallery`
2. أدخل كلمة المرور من `.env`: `SchoolAdmin2024!@#Secure`
3. يجب أن يتم تسجيل الدخول بنجاح

#### ب. اختبار رفع صورة:
1. بعد تسجيل الدخول
2. اختر صورة من جهازك
3. أدخل عنوان (مثال: "صورة تجريبية")
4. اختر تصنيف (مثال: "الفعاليات")
5. اضغط "إضافة الصورة"
6. يجب أن تظهر رسالة نجاح

#### ج. اختبار عرض الصور:
1. اذهب إلى: `http://localhost:5173/gallery`
2. يجب أن تظهر الصورة المرفوعة

#### د. اختبار حذف صورة:
1. في لوحة التحكم `/admin/gallery`
2. اضغط على أيقونة الحذف بجانب صورة
3. أكد الحذف
4. يجب أن تختفي الصورة

#### هـ. اختبار القفل الأمني:
1. حاول تسجيل الدخول بكلمة مرور خاطئة 5 مرات
2. يجب أن يتم قفل الحساب لمدة 10 دقائق

---

## ✅ النتائج المتوقعة:

- ✅ تسجيل الدخول يعمل
- ✅ رفع الصور يعمل
- ✅ عرض الصور يعمل
- ✅ حذف الصور يعمل
- ✅ القفل الأمني يعمل
- ✅ تحديث تلقائي لصفحة المعرض

---

## 🐛 حل المشاكل:

**المشكلة:** لا يمكن رفع الصور
- ✅ تأكد من تشغيل Backend (`npm run dev:server`)
- ✅ تحقق من ملف `.env` موجود
- ✅ تحقق من كلمة المرور صحيحة

**المشكلة:** الصور لا تظهر
- ✅ تحقق من `public/api/gallery.json`
- ✅ تحقق من `localStorage` في المتصفح

**المشكلة:** Backend لا يعمل
- ✅ تأكد من تثبيت المكتبات: `npm install`
- ✅ تحقق من المنفذ 3001 غير مستخدم
- ✅ راجع `server.js` للأخطاء

---

## 📝 ملاحظات:

- النظام يعمل مع أو بدون Backend
- مع Backend: أفضل أداء وحفظ
- بدون Backend: localStorage فقط

