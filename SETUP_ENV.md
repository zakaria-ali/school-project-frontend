# 🔐 إعداد ملف .env

## الخطوة 1: إنشاء ملف .env

تم إنشاء ملف `.env` من `ENV_SETUP.txt`.

## الخطوة 2: تغيير كلمة المرور

افتح ملف `.env` وغيّر كلمة المرور:

```env
# كلمة مرور قوية (12+ حرف، أرقام، رموز)
VITE_ADMIN_PASSWORD=SchoolAdmin2024!@#Secure
ADMIN_PASSWORD=SchoolAdmin2024!@#Secure
PORT=3001
```

**⚠️ مهم:** استخدم كلمة مرور قوية وفريدة!

## الخطوة 3: اختبار النظام

```bash
# 1. تثبيت المكتبات
npm install

# 2. تشغيل النظام
npm run dev:all

# 3. اختبار:
# - افتح: http://localhost:5173/admin/gallery
# - أدخل كلمة المرور من .env
# - جرب رفع صورة
# - جرب حذف صورة
```

## الخطوة 4: بعد الرفع على GitHub

### في السيرفر/الاستضافة:

1. **أنشئ ملف `.env` في السيرفر:**
   ```bash
   # في السيرفر
   nano .env
   # أو
   vi .env
   ```

2. **أضف نفس المحتوى:**
   ```env
   VITE_ADMIN_PASSWORD=SchoolAdmin2024!@#Secure
   ADMIN_PASSWORD=SchoolAdmin2024!@#Secure
   PORT=3001
   ```

3. **أو استخدم متغيرات البيئة في منصة الاستضافة:**
   - Vercel: Settings → Environment Variables
   - Netlify: Site settings → Environment variables
   - Railway: Variables tab
   - Render: Environment section

---

## ✅ التحقق من الأمان:

```bash
# تأكد من أن .env غير موجود في Git
git status
# يجب ألا يظهر .env

# تأكد من وجوده في .gitignore
cat .gitignore | grep .env
```

---

**ملاحظة:** لا ترفع ملف `.env` أبداً إلى GitHub!

