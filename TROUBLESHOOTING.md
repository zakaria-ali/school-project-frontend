# 🔧 حل مشاكل تسجيل الدخول

## المشكلة: كلمة المرور لا تعمل

### الحل 1: إعادة تشغيل السيرفر

**Vite يحتاج إعادة تشغيل لقراءة متغيرات .env:**

1. **أوقف السيرفر** (Ctrl+C)
2. **أعد تشغيله:**
   ```bash
   npm run dev:all
   ```

### الحل 2: التحقق من ملف .env

1. **افتح ملف `.env`** وتأكد من:
   ```env
   VITE_ADMIN_PASSWORD=SchoolAdmin2024!@#Secure
   ADMIN_PASSWORD=SchoolAdmin2024!@#Secure
   ```

2. **تأكد من عدم وجود مسافات:**
   - ❌ خطأ: `VITE_ADMIN_PASSWORD = SchoolAdmin2024!@#Secure`
   - ✅ صحيح: `VITE_ADMIN_PASSWORD=SchoolAdmin2024!@#Secure`

3. **تأكد من عدم وجود علامات اقتباس:**
   - ❌ خطأ: `VITE_ADMIN_PASSWORD="SchoolAdmin2024!@#Secure"`
   - ✅ صحيح: `VITE_ADMIN_PASSWORD=SchoolAdmin2024!@#Secure`

### الحل 3: استخدام Console للتحقق

1. **افتح Developer Tools** (F12)
2. **اذهب إلى Console**
3. **اكتب:**
   ```javascript
   console.log(import.meta.env.VITE_ADMIN_PASSWORD)
   ```
4. **يجب أن تظهر:** `SchoolAdmin2024!@#Secure`

### الحل 4: إعادة إنشاء ملف .env

```bash
# احذف الملف القديم
del .env

# أنشئ ملف جديد
echo VITE_ADMIN_PASSWORD=SchoolAdmin2024!@#Secure > .env
echo ADMIN_PASSWORD=SchoolAdmin2024!@#Secure >> .env
echo PORT=3001 >> .env
```

### الحل 5: استخدام كلمة مرور بسيطة للاختبار

جرب كلمة مرور بسيطة أولاً للتأكد من أن النظام يعمل:

```env
VITE_ADMIN_PASSWORD=test123
ADMIN_PASSWORD=test123
```

ثم غيّرها بعد التأكد من أن كل شيء يعمل.

---

## 🔍 خطوات التشخيص:

1. ✅ تأكد من ملف `.env` موجود في جذر المشروع
2. ✅ تأكد من إعادة تشغيل السيرفر
3. ✅ افتح Console (F12) وتحقق من المتغيرات
4. ✅ جرب كلمة مرور بسيطة أولاً

---

## ⚠️ ملاحظات:

- **Vite يقرأ متغيرات `.env` فقط عند بدء التشغيل**
- **يجب إعادة تشغيل السيرفر بعد تغيير `.env`**
- **المتغيرات التي تبدأ بـ `VITE_` فقط متاحة في Frontend**

