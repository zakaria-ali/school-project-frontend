# ⚡ حل سريع لمشكلة كلمة المرور

## المشكلة:
كلمة المرور `SchoolAdmin2024!@#Secure` لا تعمل

## الحل السريع:

### 1. أعد تشغيل السيرفر ⚠️ مهم جداً!

**Vite لا يقرأ `.env` إلا عند بدء التشغيل!**

```bash
# أوقف السيرفر (Ctrl+C)
# ثم أعد تشغيله:
npm run dev:all
```

### 2. تحقق من Console

افتح Developer Tools (F12) → Console

ستجد رسائل Debug تظهر:
- كلمة المرور المدخلة
- كلمة المرور المتوقعة
- متغير البيئة

### 3. إذا لم تعمل، جرب كلمة مرور بسيطة:

عدّل `.env`:
```env
VITE_ADMIN_PASSWORD=admin123
ADMIN_PASSWORD=admin123
```

ثم أعد تشغيل السيرفر وجرب `admin123`

### 4. إذا عملت كلمة المرور البسيطة:

المشكلة في الأحرف الخاصة `!@#`. جرب:
```env
VITE_ADMIN_PASSWORD=SchoolAdmin2024Secure
ADMIN_PASSWORD=SchoolAdmin2024Secure
```

---

## ✅ الخطوات بالترتيب:

1. **أعد تشغيل السيرفر** (الأهم!)
2. **افتح Console** (F12) وتحقق من القيم
3. **جرب كلمة مرور بسيطة** أولاً
4. **إذا عملت، غيّرها تدريجياً**

---

## 🔍 للتشخيص:

في Console، اكتب:
```javascript
import.meta.env.VITE_ADMIN_PASSWORD
```

يجب أن تظهر كلمة المرور.

