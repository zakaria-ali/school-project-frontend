# دليل الأمان - نظام إدارة معرض الصور

## ⚠️ تحذيرات أمنية مهمة:

### 1. **كلمة المرور في متغيرات البيئة**

**قبل رفع الكود إلى GitHub:**
1. أنشئ ملف `.env` في جذر المشروع
2. أضف:
   ```
   VITE_ADMIN_PASSWORD=كلمة_مرور_قوية_هنا
   ADMIN_PASSWORD=كلمة_مرور_قوية_هنا
   ```
3. تأكد من أن `.env` موجود في `.gitignore`

### 2. **حماية الكود**

**قبل رفع الكود إلى GitHub:**
- ✅ تأكد من أن كلمة المرور في `.env` وليس في الكود
- ✅ استخدم كلمة مرور قوية
- ✅ لا ترفع ملف `.env` أبداً
- ✅ راجع الكود للتأكد من عدم وجود كلمات مرور مكشوفة

### 3. **حماية إضافية**

**للإنتاج (Production):**
1. استخدم HTTPS دائماً
2. أضف نظام مصادقة أقوى (JWT)
3. استخدم قاعدة بيانات آمنة
4. أضف CSRF protection
5. فعّل rate limiting أقوى

### 4. **كيفية تغيير كلمة المرور:**

**في Frontend:**
- أضف في `.env`: `VITE_ADMIN_PASSWORD=كلمة_مرور_جديدة`

**في Backend:**
- أضف في `.env`: `ADMIN_PASSWORD=كلمة_مرور_جديدة`

### 5. **فحص الأمان قبل الرفع:**

```bash
# تأكد من عدم وجود كلمات مرور في الكود
grep -r "admin123" src/
grep -r "CHANGE_THIS" src/

# تأكد من وجود .env في .gitignore
cat .gitignore | grep .env
```

### 6. **نصائح إضافية:**

- ✅ استخدم كلمة مرور قوية (12+ حرف، أرقام، رموز)
- ✅ غيّر كلمة المرور بانتظام
- ✅ لا تشارك كلمة المرور
- ✅ استخدم 2FA إذا أمكن
- ✅ راجع سجلات الوصول بانتظام

---

## 🔒 الإعداد الآمن:

1. **أنشئ `.env`:**
   ```env
   VITE_ADMIN_PASSWORD=YourStrongPassword123!@#
   ADMIN_PASSWORD=YourStrongPassword123!@#
   PORT=3001
   ```

2. **تأكد من `.gitignore`:**
   ```
   .env
   .env.local
   .env.production
   ```

3. **لا ترفع:**
   - ❌ ملف `.env`
   - ❌ كلمات المرور في الكود
   - ❌ معلومات حساسة

---

## ✅ قائمة فحص الأمان:

- [ ] كلمة المرور في `.env` وليس في الكود
- [ ] `.env` موجود في `.gitignore`
- [ ] كلمة المرور قوية (12+ حرف)
- [ ] تم اختبار النظام قبل الرفع
- [ ] لا توجد كلمات مرور في Git history
- [ ] HTTPS مفعّل في الإنتاج
- [ ] Rate limiting مفعّل

---

**تذكر:** الأمان مسؤوليتك! تأكد من اتباع أفضل الممارسات.

