# دليل البدء السريع - نظام رفع الصور

## 🚀 التشغيل السريع:

### الخطوة 1: تثبيت المكتبات
```bash
npm install
```

### الخطوة 2: تشغيل النظام

**الخيار أ: مع Backend (موصى به)**
```bash
npm run dev:all
```

**الخيار ب: بدون Backend (localStorage فقط)**
```bash
npm run dev
```

### الخطوة 3: إعداد الأمان
1. انسخ `ENV_TEMPLATE.txt` إلى `.env`
2. غيّر كلمة المرور في `.env`

### الخطوة 4: الوصول إلى لوحة التحكم
1. افتح: `http://localhost:5173/admin/gallery`
2. أدخل كلمة المرور من ملف `.env`

---

## 📝 كيفية إضافة صورة:

1. **سجل الدخول** بكلمة المرور
2. **اختر صورة** من جهازك
3. **أدخل العنوان** (مثال: "فصل دراسي")
4. **اختر التصنيف** (الفصول الدراسية، ورش العمل، إلخ)
5. **اضغط "إضافة الصورة"**

✅ الصورة ستظهر تلقائياً في معرض الصور!

---

## 🔒 تغيير كلمة المرور:

افتح ملف `.env` وغيّر:
```env
VITE_ADMIN_PASSWORD=كلمة_مرور_جديدة_قوية
ADMIN_PASSWORD=كلمة_مرور_جديدة_قوية
```

---

## 📁 أين تُحفظ الصور؟

- **مع Backend:** `public/images/gallery/`
- **البيانات:** `public/api/gallery.json`
- **بدون Backend:** localStorage (Base64)

---

## ⚠️ ملاحظات مهمة:

1. **Backend يعمل على:** `http://localhost:3001`
2. **الموقع يعمل على:** `http://localhost:5173`
3. **للاستخدام الحقيقي:** غيّر كلمة المرور وأضف HTTPS

---

## 🆘 حل المشاكل:

**المشكلة:** الصور لا تظهر
- ✅ تأكد من تشغيل Backend (`npm run dev:server`)
- ✅ تحقق من ملف `public/api/gallery.json`

**المشكلة:** لا يمكن رفع الصور
- ✅ تأكد من حجم الصورة (أقل من 5MB)
- ✅ تأكد من نوع الملف (JPG, PNG, إلخ)

---

## 🎉 جاهز للاستخدام!

الآن يمكنك إضافة صور جديدة بسهولة دون تعديل الكود!

